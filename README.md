# ESLint config

## Setup

### Install

Install the package.

```sh
npm install eslint-config-tsuyoshi84
```

Add the package to `extends` in the ESLint config

```js
{
  "extends": ["tsuyoshi84"]
}
```
